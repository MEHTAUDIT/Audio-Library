pluginManagement {
    repositories {
        maven { url = uri("https://repo.spring.io/plugins-release") }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "audio-library-backend"
